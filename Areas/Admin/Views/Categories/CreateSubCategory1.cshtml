@model SubCategory1
@{
    ViewData["Title"] = "إضافة فئة فرعية أولى";
    var category = ViewBag.Category as Category;
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h3>إضافة فئة فرعية أولى</h3>
    <a href="/Admin/Categories" class="btn btn-outline-secondary">
        <i class="fas fa-arrow-right me-2"></i>
        العودة للقائمة
    </a>
</div>

<div class="alert alert-info">
    <i class="fas fa-info-circle me-2"></i>
    إضافة فئة فرعية جديدة تحت: <strong>@category?.Name</strong>
</div>

<div class="card-admin">
    <div class="card-body">
        <form asp-action="CreateSubCategory1" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

            <input asp-for="CategoryId" type="hidden" value="@category?.Id">

            <div class="row">
                <div class="col-md-8">
                    <div class="mb-3">
                        <label asp-for="Name" class="form-label">اسم الفئة الفرعية *</label>
                        <input asp-for="Name" class="form-control" placeholder="أدخل اسم الفئة الفرعية" required>
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                </div>

                <div class="col-md-4">
                    <div class="mb-3">
                        <label class="form-label">حالة الفئة</label>
                        <div class="form-check">
                            <input asp-for="IsActive" class="form-check-input" type="checkbox" checked>
                            <label asp-for="IsActive" class="form-check-label">
                                نشطة
                            </label>
                        </div>
                    </div>
                </div>
            </div>

            <div class="d-flex gap-2 justify-content-end">
                <a href="/Admin/Categories" class="btn btn-outline-secondary">
                    <i class="fas fa-times me-2"></i>
                    إلغاء
                </a>
                <button type="submit" class="btn btn-admin">
                    <i class="fas fa-save me-2"></i>
                    حفظ الفئة الفرعية
                </button>
            </div>
        </form>
    </div>
</div>